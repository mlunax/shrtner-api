version: "3.3"

volumes:
  db:

services:
  redis:
    image: redis:alpine
    volumes:
      - db:/db
      - ./redis.conf:/etc/redis.conf
    command: redis-server /etc/redis.conf
    ports: [6379:6379]

  shrtner:
    build: ./server
    env_file:
      - ./.env
